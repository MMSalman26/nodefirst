const arr1 = [4, 5, 6, 7, 9]
const arr2 = [1, 2, 3, 3, 5]

let termCredits = 0;

for (let i = 0; i < 4; i++) {
  if (arr1[i] > 4) {
    termCredits = termCredits + arr2[i]
  }
}

console.log(termCredits)
console.log(termCredits)
console.log(termCredits)
console.log(termCredits)